interface:
  display_name: "UE Senior Dev"
  short_description: "Run UE logic-review-build implementation pipeline"
  default_prompt: "Use $ue-senior-dev to run the full UE implementation pipeline: logic, review, and build-fix cycles."

