interface:
  display_name: "UE Build"
  short_description: "Build Unreal project targets safely"
  default_prompt: "Use $ue-build to build the current Unreal project and report only actionable errors."

